version: "3.9"

services:
  iods:
    build: .
    container_name: iods
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./iods.db
      ENV: dev
      LOG_LEVEL: INFO
      IODS_API_KEY: ""
    volumes:
      - ./:/app
